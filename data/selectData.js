/**
 * A JS object that holds the data for the selects.
 * The select data is structured as an object where each property represents a category
 * and contains an array of options.
 */
export const selectData = {
    pasta: ["spaghetti", "penne", "farfalle"],
    sauce: ["marinara", "alfredo", "pesto"],
    protein: ["chicken", "shrimp", "tofu"]
};
